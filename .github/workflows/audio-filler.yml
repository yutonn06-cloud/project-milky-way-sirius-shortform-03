name: Audio Filler

on:
  schedule:
    # 21:15 UTC = 06:15 Asia/Tokyo. Runs 15 min after the cloud rewrite routine
    # (06:00 JST) so the Notion page has its text written before we look for it.
    - cron: '15 21 * * *'
  workflow_dispatch:
    inputs:
      mode:
        description: 'pending = fill empty 音声URL_A only (default). backfill_localhost = regenerate audio for any entry whose 音声URL_A still points at http://localhost:8765/...'
        required: true
        default: pending
        type: choice
        options:
          - pending
          - backfill_localhost

permissions:
  contents: write

concurrency:
  group: audio-filler
  cancel-in-progress: false

jobs:
  fill:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Debug secret shape (does not leak values)
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          ELEVENLABS_VOICE_ID: ${{ secrets.ELEVENLABS_VOICE_ID }}
        run: |
          echo "NOTION_TOKEN       len=${#NOTION_TOKEN}       prefix=${NOTION_TOKEN:0:4}    suffix=${NOTION_TOKEN: -4}"
          echo "NOTION_DATABASE_ID len=${#NOTION_DATABASE_ID} prefix=${NOTION_DATABASE_ID:0:8}  suffix=${NOTION_DATABASE_ID: -8}"
          echo "ELEVENLABS_API_KEY len=${#ELEVENLABS_API_KEY} prefix=${ELEVENLABS_API_KEY:0:4} suffix=${ELEVENLABS_API_KEY: -4}"
          echo "ELEVENLABS_VOICE_ID len=${#ELEVENLABS_VOICE_ID}"

      - name: Run audio filler
        env:
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          ELEVENLABS_VOICE_ID: ${{ secrets.ELEVENLABS_VOICE_ID }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          BACKFILL_LOCALHOST: ${{ github.event.inputs.mode == 'backfill_localhost' }}
        run: bash tools/audio-filler.sh
